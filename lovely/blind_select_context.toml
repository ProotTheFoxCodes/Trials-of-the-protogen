[manifest]
version = "1.0.0"
priority = 0
[[patches]]
[patches.pattern]
target = "functions/button_callbacks.lua"
pattern = '''
          G.STATE = G.STATES.BLIND_SELECT
          G.CONTROLLER.locks.toggle_shop = nil
          return true
        end
      }))
'''
position = 'after'
times = 1
match_indent = true
payload = '''
      G.E_MANAGER:add_event(Event({
        trigger = "after",
        delay = 3,
        blockable = false, blocking = true,
        func = function()
          SMODS.calculate_context({totp_selecting_blind = true})
          return true
        end
      }))
'''